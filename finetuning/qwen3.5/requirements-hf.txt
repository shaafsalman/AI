# Stock HuggingFace stack — for dense_9b_hf.py and moe_35b_hf.py
#
#   pip install -r requirements-hf.txt
#
# Install into its own virtualenv; these versions conflict with the Unsloth
# stack in requirements-unsloth.txt.

torch>=2.4

# Qwen3.5 support landed after the last tagged release — install from source.
transformers @ git+https://github.com/huggingface/transformers.git

peft>=0.13
trl>=0.12
accelerate>=1.0
datasets>=3.0
safetensors>=0.4
huggingface_hub>=0.26

# optim="adamw_8bit"
bitsandbytes>=0.44
