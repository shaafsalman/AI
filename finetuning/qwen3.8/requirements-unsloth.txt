# Unsloth stack — for dense_27b_unsloth.py
#
#   pip install -r requirements-unsloth.txt
#
# Install into its own virtualenv; these versions conflict with the stock
# HuggingFace stack in ../qwen3.5/requirements-hf.txt.

torch>=2.4

unsloth
unsloth_zoo

# Qwen3.8 support landed after the last tagged release — install from source.
transformers @ git+https://github.com/huggingface/transformers.git

trl>=0.12
peft>=0.13
accelerate>=1.0
datasets>=3.0
safetensors>=0.4
huggingface_hub>=0.26

# optim="adamw_8bit"
bitsandbytes>=0.44
